# TODO.

This will define what should be in a Jira story, what issue type it should be etc.

shoudl define acceptance critera.

Should define any new E2E tests that should be included that will then be done by the dev.md subagent. DoD.

Unit test are defined by the dev agent itself.
