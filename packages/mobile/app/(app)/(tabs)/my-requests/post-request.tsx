// Mounts the same post-request screen inside the My requests stack so that
// back-nav from submission returns to the My requests list rather than
// bouncing over to the Home tab's stack that also hosts this screen.
export { default } from "../(stack)/post-request";
